import React from "react"
import { <%typeName%> } from "ss-schema"

const Block: React.FC<{ element: <%typeName%> }> = ({ element }) => {

    return <div>{element.__typename}</div>

}

export default Block